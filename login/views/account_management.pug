//Delete Account
//Change Account Name
// 비밀번호 변경
    계정 삭제
    계정 정보(전적검색)
doctype html
html
    head
        meta(charset='utf-8')
        |
        title Login
    |
    body
        h1 Login
        |
        form(action='/login', method='post')
            | Id :
            input(type='text', name='login_id')
            br
            | Password :
            input(type='text', name='login_password')
            br
            |
            input(type='submit')








doctype html
    html
        head
            meta(charset='utf-8')
            title Chat
            style.
                .login {
                    width: 49%;
                    height: 99vh;
                    font-size: 99px;
                }

                .register {
                    width: 49%;
                    height: 99vh;
                    font-size: 99px;
                }
        body
            button.login(onclick="location.href='/login'")
                | 로그인
            button.register(onclick="location.href='/register'")
                | 회원가입
