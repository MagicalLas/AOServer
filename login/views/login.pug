doctype html
html
    head
        meta(charset='utf-8')
        |
        title Login
    |
    body
        h1 Login
        |
        form(action='/login', method='post')
            | Id :
            input(type='text', name='login_id')
            br
            | Password :
            input(type='text', name='login_password')
            br
            |
            input(type='submit')